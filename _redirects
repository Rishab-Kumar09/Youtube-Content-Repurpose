# Redirects for YouTube Content Repurpose App
# This file is used by Netlify to handle redirects and rewrites

# Handle API requests (if any future API endpoints are needed)
/api/* /netlify/functions/:splat 200

# Handle form submissions (if any future forms are added)
/submit-form /netlify/functions/submit-form 200

# Fallback for single page application behavior
/* /index.html 200 